services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: dscr
      POSTGRES_PASSWORD: dscr
      POSTGRES_DB: dscr_verdict
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
